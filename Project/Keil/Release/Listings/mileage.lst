C51 COMPILER V9.60.7.0   MILEAGE                                                           08/05/2025 17:55:49 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE MILEAGE
OBJECT MODULE PLACED IN .\Release\Objects\mileage.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\mileage.c LARGE OPTIMIZE(9,SIZE) BROWSE ORDER INTVECTOR(0X000
                    -C) INCDIR(..\..\Libraries\Include;..\..\User;..\..\User\lib;..\..\Hardware) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Relea
                    -se\Listings\mileage.lst) OBJECT(.\Release\Objects\mileage.obj)

line level    source

   1          // encoding UTF-8
   2          // mileage.c
   3          #include "mileage.h"
   4          
   5          volatile u16 mileage_save_time_cnt = 0; // 里程扫描所需的计数值,每隔一定时间将里程写�
             -�flash
   6          volatile u32 distance = 0;              // 存放每次扫描时走过的路程（单位：毫米）-->用
             -于里程表的计数
   7          
   8          volatile u16 mileage_update_time_cnt = 0; // 里程更新的时间计数,每隔一段时间更新一次当
             -前里程（负责控制发送里程的周期）
   9          
  10          // 总里程扫描
  11          void mileage_scan(void)
  12          {
  13   1          // 下面这组变量用来控制每走过一段距离时，发送里程数据
  14   1          static u32 old_total_mileage = 0;      // 用来记录旧的大计里程的变量
  15   1          static u32 old_subtotal_mileage = 0;   // 用来记录旧的小计里程的变量
  16   1          static u32 old_subtotal_mileage_2 = 0; // 用来记录旧的小计里程2的变量
  17   1      
  18   1          /*
  19   1              是否有里程数据需要保存的标志变量，0--没有里程变化，不需要保存，1--�
             -�里程变化，需要保存
  20   1              目前每过1m就会置位一次，保存之后清零
  21   1          */
  22   1          static bit flag_is_any_mileage_save = 0;
  23   1      
  24   1          /*
  25   1              速度为0，里程变化一次就保存一次
  26   1              速度不为0， 每30s且里程有变化，则保存一次，每走过100m也保存一次
  27   1          */
  28   1          if (mileage_save_time_cnt >= 30000) // 30 000 ms -- 30s
  29   1          {
  30   2              mileage_save_time_cnt = 0;
  31   2      
  32   2              if (fun_info.speed > 0 && flag_is_any_mileage_save)
  33   2              {
  34   3                  // 速度大于0且里程有变化
  35   3                  fun_info_save(); // 将 fun_info 写回flash
  36   3                  flag_is_any_mileage_save = 0;
  37   3              }
  38   2          }
  39   1      
  40   1          // 如果速度==0，里程有变化，每 1s 写入一次flash
  41   1          if (0 == fun_info.speed &&           /* 当前速度为0 */
  42   1              mileage_save_time_cnt >= 1000 && /* 1s 后 */
  43   1              flag_is_any_mileage_save)        /* 里程有变化，需要保存 */
  44   1          {
  45   2              fun_info_save();
  46   2              flag_is_any_mileage_save = 0;
  47   2              mileage_save_time_cnt = 0;
  48   2          }
  49   1      
C51 COMPILER V9.60.7.0   MILEAGE                                                           08/05/2025 17:55:49 PAGE 2   

  50   1          if (distance >= 1000) // 1000mm -- 1m
  51   1          {
  52   2              // 如果走过的距离超过了1m，再进行保存（保存到变量） 
  53   2              if (fun_info.save_info.total_mileage < (u32)(999999 * 1000)) // 99 9999 KM
  54   2              {
  55   3                  fun_info.save_info.total_mileage++; // +1m
  56   3              } 
  57   2              
  58   2              if (fun_info.save_info.subtotal_mileage < (u32)(9999999)) // 9999.9KM， 9999 999 m
  59   2              {
  60   3                  fun_info.save_info.subtotal_mileage++; // +1m
  61   3              }
  62   2      
  63   2              if (fun_info.save_info.subtotal_mileage_2 < (u32)(9999999)) // 9999.9KM， 9999 999 m
  64   2              {
  65   3                  fun_info.save_info.subtotal_mileage_2++; // +1m
  66   3              }
  67   2      
  68   2              flag_is_any_mileage_save = 1; // 表示需要把里程输入写入到flash
  69   2              distance -= 1000;             // 剩下的、未保存的、不满1m的数据留到下一次再保
             -存
  70   2      
  71   2              {
  72   3                  static cnt = 0;
  73   3                  cnt++;
  74   3                  if (cnt >= 100)
  75   3                  {
  76   4                      cnt = 0;
  77   4      
  78   4                      // 进入到这里，说明走过了100m
  79   4                      fun_info_save();
  80   4                      flag_is_any_mileage_save = 0;
  81   4                      mileage_save_time_cnt = 0;
  82   4                  }
  83   3              }
  84   2          }
  85   1      
  86   1      #ifdef USE_INTERNATIONAL /* 公制单位 */
  87   1                               // 如果大计里程有变化且超过了100m(不能满1000m再发送，在显�
             -�上，会先更新大计里程，过几百ms才更新小计里程)
  88   1          if ((fun_info.save_info.total_mileage - old_total_mileage) > 100)
  89   1      #else /* USE_IMPERIAL // 英制单位 */
                  // 如果大计里程有变化且超过了161m(0.1英里)
                  if ((fun_info.save_info.total_mileage - old_total_mileage) > 161)
              #endif
  93   1          {
  94   2              old_total_mileage = fun_info.save_info.total_mileage; // 记录旧的里程
  95   2      
  96   2              // printf("total mileage: %lu m\n", fun_info.save_info.total_mileage);
  97   2      
  98   2              // 发送数据的操作，可以先置标志位
  99   2              flag_get_total_mileage = 1;
 100   2          }
 101   1      
 102   1      #ifdef USE_INTERNATIONAL /* 公制单位 */
 103   1                               // 如果小计里程有变化且超过了100m
 104   1          if ((fun_info.save_info.subtotal_mileage - old_subtotal_mileage) > 100)
 105   1      #else /* USE_IMPERIAL // 英制单位 */
                  // 如果小计里程有变化且超过了161m(0.1英里)
                  if ((fun_info.save_info.subtotal_mileage - old_subtotal_mileage) > 161)
              #endif
 109   1          {
C51 COMPILER V9.60.7.0   MILEAGE                                                           08/05/2025 17:55:49 PAGE 3   

 110   2              old_subtotal_mileage = fun_info.save_info.subtotal_mileage; // 记录旧的里程
 111   2      
 112   2              // printf("subtotal mileage: %lu m\n", fun_info.save_info.subtotal_mileage);
 113   2      
 114   2              // 发送数据的操作，可以先置标志位
 115   2              flag_get_sub_total_mileage = 1;
 116   2          }
 117   1      
 118   1          if ((fun_info.save_info.subtotal_mileage_2 - old_subtotal_mileage_2) > 100)
 119   1          {
 120   2              old_subtotal_mileage_2 = fun_info.save_info.subtotal_mileage_2; // 记录旧的里程
 121   2      
 122   2              // printf("subtotal mileage_2: %lu m\n", fun_info.save_info.subtotal_mileage_2);
 123   2      
 124   2              // 发送数据的操作，可以先置标志位
 125   2              flag_get_sub_total_mileage_2 = 1;
 126   2          }
 127   1      
 128   1          if (mileage_update_time_cnt >= MILEAGE_UPDATE_TIME_MS)
 129   1          {
 130   2              // 每隔一段时间，发送大小里程，
 131   2              // 因为最后大计里程在999999km,小计里程在999.9km之后，就不更新了，
 132   2              // 要在刷新一次，才会发送1000000km和1000.0km的大小里程
 133   2              mileage_update_time_cnt = 0;
 134   2              flag_get_total_mileage = 1; //
 135   2              flag_get_sub_total_mileage = 1;
 136   2              flag_get_sub_total_mileage_2 = 1;
 137   2          }
 138   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    395    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =     22    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =      1    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
