# 摩托车仪表盘项目功能说明文档

## 项目概述
这是一个基于LCD显示屏的摩托车仪表盘系统，能够检测多种摩托车运行参数并通过UART接口与外部设备通信。系统支持多种输入方式（触摸按键、AD按键）以及功能扩展。

## 系统架构

### 硬件层 (Hardware 目录)
- [adc.c/h](Hardware/adc.h) - ADC采样模块，用于电池电压、油量等模拟信号采集
- [aip1302.c/h](Hardware/aip1302.h) - AIP1302时钟芯片驱动
- [iic_soft.c/h](Hardware/iic_soft.h) - 软件IIC实现
- [key_driver.c/h](Hardware/key_driver.h) - 按键驱动
- [tmr1.c/h](Hardware/tmr1.h) - 定时器1，用于检测脉冲计时
- [tmr2.c/h](Hardware/tmr2.h) - 定时器2，用于扫描电平变化脉冲
- [uart0.c/h](Hardware/uart0.h) - UART0通信接口

### 库文件 (Libraries 目录)
- Include - 芯片底层库函数和寄存器定义
- Source - 系统初始化和调试相关代码

### 用户逻辑 (User 目录)
包含摩托车仪表的所有功能模块：

## 功能模块详解

### 1. 输入控制模块
- **触摸按键检测** ([touch_key.c/h](User/touch_key.h)) - 支持物理触摸按键输入
- **AD按键检测** ([ad_key.c/h](User/ad_key.h)) - 支持通过ADC检测按键
- **引脚电平检测** ([pin_level_scan.c/h](User/pin_level_scan.h)) - 检测刹车、转向灯、档位等开关信号

### 2. 传感器检测模块
- **速度检测** ([speed_scan.c/h](User/speed_scan.h)) - 检测摩托车时速
- **发动机转速检测** ([engine_speed_scan.c/h](User/engine_speed_scan.h)) - 检测发动机转速(RPM)
```
每经过 ENGINE_SPEED_SCAN_UPDATE_TIME，计算一次发动机转速，并直接发送数据
```
- **电池检测** ([battery.c/h](User/battery.h)) - 检测电池电压及电量
- **油量检测** ([fuel_capacity.c/h](User/fuel_capacity.h)) - 检测摩托车油量
```
主函数调用 fuel_capacity_scan() 

fuel_capacity_scan() 函数：
每次检测对应的ad值-> 计算油量百分比 -> 计算油量挡位
刚上电时，获取一次油量百分比、计算油量挡位，作为初始值
每隔 FUEL_UPDATE_TIME 个时间间隔，计算并比较当前油量挡位和上次记录的油量挡位
如果比较发现挡位相同，则发送油量百分比数据，不更新挡位
如果比较发现挡位不同，则给对应标志位置一，让定时器累计时间

当定时器累计计时 FUEL_UPDATE_LAST_FUEL_PERCENTAGE_TIME 个时间后，
更新油量挡位，每次更新油量挡位只变化1档，再计算出油量挡位对应的油量百分比，
最好发送油量百分比数据

最后得到的油量百分比是非线性的，但是只需要油量格数跟客户提供的数据尽量相似就行

```

- **水温检测** ([temp_of_water.c/h](User/temp_of_water.h)) - 检测发动机水温（可选）
- **里程计算** ([mileage.c/h](User/mileage.h)) - 计算并管理总里程和小计里程

### 3. 数据通信模块
- **指令处理** ([instruction.c/h](User/instruction.h)) - 处理来自MP5的指令
- **数据发送** ([send_data.c/h](User/send_data.h)) - 向MP5发送检测到的数据

### 4. 时间管理模块
- **AIP1302时钟** ([aip1302.c/h](User/aip1302.h)) - 实时时钟功能

### 5. 数据存储模块
- **功能信息管理** ([fun_info.c/h](User/fun_info.h)) - 管理所有功能状态和数据

## 通信协议

### 指令集定义
系统支持以下指令与MP5通信：
- `0x80`: 获取所有功能状态
- `0x21`: 获取档位状态
- `0x22`: 获取电池电量
- `0x23`: 获取刹车状态
- `0x24`: 获取左转向灯状态
- `0x25`: 获取右转向灯状态
- `0x26`: 获取远光灯状态
- `0x27`: 获取发动机转速
- `0x28`: 获取时速
- `0x29`: 获取油量
- `0x8B`: 获取总里程
- `0x8C`: 获取小计里程
- `0x8E`: 修改时间
- `0x98`: 清除总里程
- `0x99`: 清除小计里程

### 档位定义
- `GEAR_NEUTRAL`: 空挡
- `GEAR_FIRST`: 一档
- `GEAR_SECOND`: 二档
- `GEAR_THIRD`: 三档
- `GEAR_FOURTH`: 四档
- `GEAR_FIFTH`: 五档
- `GEAR_SIXTH`: 六档
- `GEAR_UNKNOWN`: 未知档位

## 配置选项 (my_config.h)

### 功能启用配置
- `TOUCH_KEY_ENABLE`: 触摸按键检测功能
- `AD_KEY_ENABLE`: AD按键检测功能
- `SPEED_SCAN_ENABLE`: 速度检测功能
- `ENGINE_SPEED_SCAN_ENABLE`: 发动机转速检测功能
- `BATTERY_SCAN_ENABLE`: 电池检测功能
- `FUEL_CAPACITY_SCAN_ENABLE`: 油量检测功能
- `PIN_LEVEL_SCAN_ENABLE`: 引脚电平检测功能
- `IC_1302_ENABLE`: AIP1302时钟IC功能
- `TEMP_OF_WATER_SCAN_ENABLE`: 水温检测功能

### 单位设置
- `USE_INTERNATIONAL`: 使用国际单位制

### 时间配置
- `MILEAGE_UPDATE_TIME_MS`: 里程更新时间（5秒）
- `TEMP_OF_WATER_UPDATE_TIME_MS`: 水温更新时间（1秒）

## 数据存储
系统使用EEPROM存储以下数据：
- 总里程（0~99999.9KM）
- 小计里程（0~999.9KM）
- 小计里程2（0~999.9KM）
- 时钟数据（如果启用AIP1302）

## 系统初始化流程
1. 系统基础初始化（包括看门狗配置）
2. GPIO引脚配置
3. UART0串口初始化
4. 定时器配置（TMR1、TMR2）
5. ADC配置（如启用相关功能）
6. I2C接口配置
7. EEPROM数据读取
8. 功能信息初始化

## 维护说明
- 代码采用模块化设计，各功能独立成模块便于维护
- 使用标志位机制进行任务调度，提高响应效率
- 支持数据持久化存储，断电后数据不丢失
- 提供完整的错误检测和故障处理机制